export enum InputFields {
    EMAIL = "email",
    FIRST_NAME = "firstname",
    LAST_NAME = "lastname",
    PASSWORD = "password",
}

export enum FormFields {
    EMAIL = "email",
    FIRST_NAME = "firstname",
    LAST_NAME = "lastname",
    PASSWORD = "password",
    IS_SUBSCRIBED = "is_subscribed",
    PRIVACY_POLICY = "privacy_policy",
}

export enum WarningMessages {
    EMAIL_WARNING = "Wprowadź poprawne znaki",
    FIELD_REQUIRED = "To pole jest wymagane",
    POLICY_CHECK_REQUIRED = "Akceptacja regulaminu jest wymagana"
}

export enum Color {
    BROWN = "brązowy",
    BLACK = "czarny",
    BLUE = "niebieski",
    GREEN = "zielony",
}

export enum Price {
    UP_TO_75 = "do 75 PLN",
    FROM_75_TO_150 = "od 75 do 150 PLN",
    UP_TO_200 = "od 200 PLN",
}

export enum Size {
    S = "S",
    M = "M",
    L = "L",
}
